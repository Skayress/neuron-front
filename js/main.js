const toolsTabs = new ItcTabs('.tools-tabs')
const newsTabs = new ItcTabs('.news-tabs')

const swiper = new Swiper('.benefits-slider', {

	// If we need pagination
	pagination: {
		el: '.swiper-pagination',
	},
});